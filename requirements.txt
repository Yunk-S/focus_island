# Focus Island 后端依赖
# 人脸检测/识别基于 PyPI 包 uniface（yakhyo/uniface）

# 核心依赖
numpy>=1.24.0
opencv-python>=4.8.0
# uniface 依赖 onnxruntime；需要 NVIDIA GPU 时可改为: pip install onnxruntime-gpu 并卸载 onnxruntime
onnxruntime>=1.16.0
# RetinaFace / SCRFD / ArcFace 等（此前漏写会导致 ModuleNotFoundError: uniface）
uniface>=3.0.0,<4

# WebSocket 服务
websockets>=12.0

# REST API 服务
fastapi>=0.104.0
uvicorn>=0.24.0

# 配置
pyyaml>=6.0

# 数据序列化
pydantic>=2.0.0

# 开发依赖
pytest>=7.4.0
pytest-asyncio>=0.21.0
